{
  "name": "hayzed-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd app && npm install && npm run dev",
    "build": "cd app && npm install && npm run build",
    "install": "cd app && npm install"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.12",
    "typescript": "^5.8.3",
    "vite": "^6.3.1"
  }
}
